export function Logo(props: { small?: boolean }) {
  return (
    <div class="h-8 w-max bg-background invert rounded-sm flex flex-row items-center justify-center p-2 font-black text-xs uppercase">
      TK
    </div>
  );
}
